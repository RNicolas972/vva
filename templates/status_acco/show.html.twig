{% extends 'base.html.twig' %}

{% block title %}StatusAcco{% endblock %}

{% block body %}
    <div class="form-inline text-right">
        <h1 class="mr-auto">Etat hébergment</h1>
        <a href="{{ path('status_acco_edit', {'id': status_acco.id}) }}" class="btn btn-sm btn-warning mx-1">edit</a>
        {{ include('status_acco/_delete_form.html.twig') }}
    </div>
    <table class="table my-5">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status_acco.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ status_acco.name }}</td>
            </tr>
        </tbody>
    </table>
    <div class="text-right">        
        <a href="{{ path('status_acco_index') }}" class="btn">back to list</a>
    </div>

{% endblock %}
