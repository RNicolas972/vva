<form method="post" action="{{ path('status_acco_delete', {'id': status_acco.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ status_acco.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
