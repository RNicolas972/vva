{% extends 'base.html.twig' %}

{% block title %}TypeAcco{% endblock %}

{% block body %}
    <h1>TypeAcco</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_acco.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_acco.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_acco_index') }}">back to list</a>

    <a href="{{ path('type_acco_edit', {'id': type_acco.id}) }}">edit</a>

    {{ include('type_acco/_delete_form.html.twig') }}
{% endblock %}
