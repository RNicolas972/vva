<form method="post" action="{{ path('type_acco_delete', {'id': type_acco.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_acco.id) }}">
    <button class="btn">Delete</button>
</form>
